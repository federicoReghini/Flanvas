import { PixelRatio, StyleSheet } from "react-native";

export const menu_styles = StyleSheet.create({

    menu: {
        marginTop: 30
    },
    voiceMenu: {
        marginTop: 10
    },
    paletteWrapper: {
        width: 40,
        height: 40,
    },
})